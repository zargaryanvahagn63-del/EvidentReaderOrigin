<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.EvidentReader" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.EvidentReader" parent="Base.Theme.EvidentReader" />

    <style name="Theme.EvidentReader.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.EvidentReader.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.EvidentReader.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>